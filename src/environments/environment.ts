import { Environment } from 'src/entities/environment.entity';

export const environment: Environment = {
  production: false,
  challengeApiProtocol: 'http://',
  challengeApiDomain: 'localhost',
  challengeApiPort: 4200
};
