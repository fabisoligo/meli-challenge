/**
 * Categoría de un producto.
 */
export interface ProductCategory {

    /** Identificador. */
    id: string;

    /** Nombre */
    name: string;

    /** Cantidad de resultados. */
    results: number;
}